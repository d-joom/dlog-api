server:
  port: 8080
spring:
#  config:
#    import: optional:configserver:http://54.81.247.178:8081 # import:optional:configserver사용 -> Spring cloud config server 주소 
#  application:
#    name: dlog
#  profiles:
#    active: local #환경 선택
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dlog
    username: jumin
    password: wbakswlDlog10!
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: trace